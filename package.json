{
  "name": "wotd",
  "version": "3.1.0",
  "description": "WOTD is a binary that look for and downloads the word of the day from the selected dictionary. By default, the dictionary is choose based on operating system language. The language can be manually set.",
  "exports": "./main.js",
  "bin": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clean && npm run build && node dist/main.js",
    "start:dev": "npm run compile && node dist/main.js",
    "build": "npm ci && npm run compile",
    "clean": "rimraf dist release node_modules",
    "compile": "tsc -p tsconfig.json",
    "pack": "npm run clean && npm run build && pkg --out-path release .",
    "pack:linux": "npm run clean && npm run build && pkg -t node16-linux-x64 --out-path release .",
    "pack:macos": "npm run clean && npm run build && pkg -t node16-macos-x64 --out-path release .",
    "pack:win": "npm run clean && npm run build && pkg -t node16-win-x64 --out-path release ."
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/raaowx/wotd.git"
  },
  "author": "raaowx",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/raaowx/wotd/issues"
  },
  "homepage": "https://github.com/raaowx/wotd",
  "dependencies": {
    "axios": "^0.22.0",
    "cheerio": "^1.0.0-rc.10",
    "os-locale": "^6.0.1",
    "yargs": "^17.2.1"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.30",
    "@types/node": "^16.10.3",
    "@types/yargs": "^17.0.3",
    "pkg": "^5.3.3",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "type": "module",
  "engines": {
    "node": "^12.20.0 || ^14.13.1 || >=16.0.0"
  }
}
